eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/



  datasource:
    url: jdbc:mysql://localhost:3306/restaurantdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: "Phanindra@123"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    # With Spring Boot 3 / Hibernate 6, you can omit dialect completely.
    # If you keep it, use the non-deprecated class:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
